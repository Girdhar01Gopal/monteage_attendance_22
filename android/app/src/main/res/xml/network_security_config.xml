<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config
  - WSS (wss://) to att.whatido.in is ALLOWED (secure WebSocket over TLS)
  - WS (ws://) cleartext is BLOCKED everywhere except local dev
-->
<network-security-config>

    <!-- Production server: only encrypted TLS traffic allowed -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">att.whatido.in</domain>
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </domain-config>

    <!-- Local dev / emulator: allow cleartext for testing -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!-- Base config: block all other cleartext by default -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

</network-security-config>